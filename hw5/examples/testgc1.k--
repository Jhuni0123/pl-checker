let x := 0 in
let i := 0 in
let proc fib (x) =
    let tmp := 0 in
    let tmp2 := 0 in
    let tmp3 := 0 in
    i := i + 1;
    if x < 2 then x
    else fib(x-1) + fib(x-2) + tmp + tmp2 + tmp3
in
let proc fib2 (x) =
    let tmp := 0 in
    let tmp2 := 0 in
    let tmp3 := 0 in
    i := i + 1;
    if x < 2 then x
    else fib(x-1) + fib(x-2) + tmp + tmp2 + tmp3
in
write(fib(14)+x);
write(fib2(14)+fib(0)+x)
